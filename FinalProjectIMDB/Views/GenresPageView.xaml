<UserControl x:Class="FinalProjectIMDB.Views.GenresPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FinalProjectIMDB.Views"
             xmlns:vm="clr-namespace:FinalProjectIMDB.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">


    <UserControl.DataContext>
        <vm:GenresPageViewModel/>
    </UserControl.DataContext>

    <UserControl.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="DarkBlue" Offset="0"/>
            <GradientStop Color="Purple" Offset="1"/>
        </LinearGradientBrush>
    </UserControl.Background>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>



        <Grid>
            <StackPanel VerticalAlignment="Top">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Border Style="{StaticResource sectionBorderStyle}">
                        <StackPanel>
                            <Border Background="DarkBlue" Style="{StaticResource sectionBorderStyle}">
                                <TextBlock Text="Genres" Foreground="Yellow" HorizontalAlignment="Center" FontSize="54" FontWeight="Bold" FontFamily="Cooper Black"/>

                            </Border>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="Explore the best film genres!"/>
                    <TextBlock Text="Call Now! Order Today! No Money Down!"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
